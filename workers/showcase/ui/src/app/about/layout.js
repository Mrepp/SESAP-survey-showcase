export const metadata = {
    title: 'About',
    description: 'About the purpose of the Student Experience Story Archive Project.',
}

export default function AboutLayout({ children }) {
    return children
}