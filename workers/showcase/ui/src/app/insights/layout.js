export const metadata = {
    title: 'Insights',
    description: 'Data visualizations showing themes, correlations, and demographic data.',
}

export default function InsightsLayout({ children }) {
    return children
}