export const metadata = {
    title: 'Timeline',
    description: 'A timeline of all events from all interviews, plotted against the academic timeline from pre-college through post-college.',
}

export default function TimelineLayout({ children }) {
    return children
}